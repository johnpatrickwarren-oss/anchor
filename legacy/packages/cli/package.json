{
  "name": "@anchor/cli",
  "version": "0.0.1",
  "type": "module",
  "description": "Operator CLI for the Anchor tool — run/route a disciplined role cycle and manage the memorial.",
  "license": "Apache-2.0",
  "engines": { "node": ">=24" },
  "files": ["dist"],
  "bin": { "anchor": "./src/cli.ts" },
  "publishConfig": {
    "access": "public",
    "bin": { "anchor": "./dist/cli.js" }
  },
  "dependencies": {
    "@anchor/core": "workspace:*",
    "@anchor/runtime-agent-sdk": "workspace:*"
  },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "build": "tsc -p tsconfig.build.json",
    "prepack": "pnpm run build"
  }
}
